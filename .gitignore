# OS
.DS_Store
Thumbs.db

# Secrets / env
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets.json
credentials.json
# Real Supabase config stays local; commit the .example only.
autoswap/front/supabase-config.js

# Node (if tooling is added later)
node_modules/
*.tsbuildinfo

# Supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# Logs and backups
*.log
*.bak

# Local preview / static-server scratch
**/.static-server-*.log
**/.chrome-verify-profile/

# Design-review screenshots (regenerated on demand via tools/shoot*.mjs)
autoswap/tools/shots/

# Harness / preview scratch
.claude/
.agents/
.scratch/

# Agent/skill tooling state (local only)
.codex/
.impeccable/
.claude/
skills-lock.json
