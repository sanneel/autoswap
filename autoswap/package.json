{
  "name": "autoswap",
  "version": "1.0.0",
  "private": true,
  "description": "AutoSwap — Georgian car exchange marketplace (Supabase + static frontend)",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:db": "psql \"$LOCAL_DB_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/offer-flow.test.sql",
    "test:db:otp": "psql \"$LOCAL_DB_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/otp-rate-limit.test.sql",
    "test:db:security": "psql \"$LOCAL_DB_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/security-hardening.test.sql",
    "functions:stage": "node scripts/stage-functions.mjs",
    "build": "node scripts/gen-config.mjs && node scripts/hash-assets.mjs",
    "smoke": "node scripts/smoke-frontend.mjs",
    "ingest:catalog": "node scripts/ingest-car-catalog.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.56.0"
  }
}
